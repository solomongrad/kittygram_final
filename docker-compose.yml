version: '3'

volumes:
  pg_data:
  volume:

services:
  db:
    image: postgres:13
    env_file: .env
    volumes:
      - pg_data:/var/lib/postgresql/data
  backend:
    image: solomongrad/kittygram_backend
    env_file: .env
    volumes:
      - static_volume:/backend_static
  frontend:
    image: solomongrad/kittygram_frontend
    env_file: .env
    command: cp -r /app/build/. /static/
    volumes:
      - static:/static
  gateway:
    image: solomongrad/kittygram_gateway
    env_file: .env
    volumes:
      - static:/static
    ports:
      - 9000:80
